var assert = require("assert");
var assesment = require("../src/task01");

describe("total #start_test", function() {
  it("should return percent #end_test", function() {
    assert.equal(assesment(), 50);
  });
});
